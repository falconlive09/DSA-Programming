#include<bits/stdc++.h>

using namespace std;

class Solution {
public:
    Solution()
    {
        ios::sync_with_stdio(false);
        cin.tie(0);
    }
    vector<string> findRelativeRanks(vector<int>& score) {
        priority_queue<pair<int,int>>pq{score.begin(),score.end()};
        vector<string>rank;
        for(int i=0;i<score.size();i++)
        {

        }

        
    }
};